[project]
name = "awesome-feature-navigation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gtsam>=4.2",
    "numpy<2",
    "pytest>=9.0.1",
]
